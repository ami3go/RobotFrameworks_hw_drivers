# Windows Setup

Run `scripts/setup_venv.ps1`, then `scripts/run_tests.ps1`. Windows Defender or
firewall rules may need to allow outbound TCP port 2049 for real chambers.
Simulator operation needs no network access.
