# v26.05 Change History

**Date:** 2026-07-31  
**Type:** Robot test hotfix

## Fixed

- `tests/hardware/smoke_test.robot` now imports Robot Framework's `Collections` library, which owns the `Log Dictionary` keyword.
- Active hardware and acceptance suites now import the canonical library path `rf_votsch_climate_chamber.library.VotschClimateChamberLibrary`.

## Regression prevention

- Added `tests/unit/test_robot_suite_dependencies.py`.
- The regression test scans Robot sources and fails when `Log Dictionary` is used without `Collections`.
- The regression test also prevents active verification suites from returning to the deprecated package import.

## Compatibility

- Public Robot keyword API unchanged.
- Chamber protocol unchanged.
- Configuration schema unchanged.
- Safety behavior unchanged.
