# v26.05 Code Review

## Finding

The hardware smoke suite called `Log Dictionary` without importing Robot Framework's `Collections` standard library. Robot therefore completed chamber communication but failed during result logging.

## Correction

1. Added `Library    Collections` to `tests/hardware/smoke_test.robot`.
2. Switched active test suites to the canonical package import.
3. Added static regression checks that run without chamber hardware and without Robot Framework runtime availability.

## Risk assessment

- Device-control risk: none; no driver or protocol code changed.
- API compatibility risk: none; keyword inventory unchanged.
- Test risk: reduced; the missing standard-library dependency is now checked automatically.

## Verdict

Approved as a patch release after Python regression tests, metadata generation, simulator smoke, wheel installation, and archive validation pass. Real-chamber rerun remains the final confirmation for this reported failure.
