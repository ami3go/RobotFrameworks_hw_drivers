# v26.04 Security Review

No credentials are used or stored. Plugin metadata and example configuration
contain no real bench addresses. Diagnostics serialize bounded structured data.
TCP endpoints are explicit; no automatic network scanning exists. Configuration
profile names are constrained and writes are atomic. Dependency and SBOM records
are included, but external vulnerability scanning is NOT_RUN in this environment.
