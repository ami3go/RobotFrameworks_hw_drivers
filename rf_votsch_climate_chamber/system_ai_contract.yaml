schema:
  id: RFDS-018
  version: "1.0"
  contract_type: AI_TEST_BENCH_CONTRACT
identity:
  bench_name: Standalone Vötsch Climate Chamber Integration Template
  status: TEMPLATE_REQUIRES_OPERATOR_COMPLETION
  contract_version: "1.0"
  driver_release: "26.08"
available_drivers:
  - plugin_id: votsch.climate_chamber
    version: "26.08"
    robot_library: rf_votsch_climate_chamber.library.VotschClimateChamberLibrary
    ai_contract: ai/votsch_climate_chamber_ai_contract.yaml
    role: environmental_temperature_control
    required: true
physical_topology:
  chamber_endpoint: UNKNOWN
  dut_location: UNKNOWN
  reference_sensor: UNKNOWN
shared_resources:
  - id: chamber_environment
    type: exclusive
    owner: votsch.climate_chamber
signal_graph:
  - source: chamber_air
    destination: dut_environment
    quantity: temperature
    status: UNKNOWN_UNTIL_BENCH_VALIDATION
scheduling:
  chamber_session_exclusive: true
  stabilization_required_before_dut_measurement: true
global_safety:
  control_enabled: false
  enable_condition: operator_completed_bench_contract_and_safety_review
  emergency_sequence:
    - Safe Shutdown
    - Disconnect All
  unknown_values_prohibit_state_change: true
limitations:
  - Exact chamber model and firmware are UNKNOWN.
  - Auxiliary output mapping is UNKNOWN.
  - DUT fixture, thermal limits, and emergency hardware are site-specific.
