# Installation

Python 3.11 or newer is required. Create a virtual environment and install the
project with `python -m pip install -e .`. Robot Framework 7.x or 8.x and
jsonschema 4.x are declared runtime dependencies. The package can be inspected
without Robot Framework in the source build environment, but Robot suites and
Libdoc require the declared dependency.
