# Examples

Thirteen canonical examples are indexed in `examples/index.yaml` and default to
simulator resources. Example 13 demonstrates bounded safe shutdown. No example
uses removed API 2 aliases or the removed Python package.

The auxiliary-output example must not be transferred to a real chamber until
digital-output mapping has been verified for that exact model and firmware.
