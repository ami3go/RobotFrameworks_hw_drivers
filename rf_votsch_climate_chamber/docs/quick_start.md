# Quick Start

Use `Connect    SIM::default`, then call `Measure Temperature`. Always add
`Disconnect All` as suite teardown. For state-changing workflows add
`Safe Shutdown` before disconnect. See `examples/01_basic_connection.robot`.
