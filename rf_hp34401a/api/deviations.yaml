deviations:
- id: HP34401A-DEV-001
  rules:
  - RFDS-003:R-003-1041
  status: OPEN
  reason: The shared rfds-core distribution is not available in the supplied project
    environment. Integration is declared as an optional dependency and remains required
    before P1.
  release_effect: Release class limited to D0 until official Robot conformance is
    executed.
- id: HP34401A-DEV-002
  rules:
  - RFDS-004:v2.0 canonical byte transport
  status: OPEN
  reason: The reviewed 1.2.8 core uses its established line-oriented transport abstraction.
    A transport-v2 migration is a separately reviewed architecture change.
  release_effect: Release class limited to D0 until official Robot conformance is
    executed; protocol boundary remains covered by FakeTransport and the real-HIL
    suite source.
- id: HP34401A-DEV-003
  rules:
  - RFDS-001 representative real-device validation
  status: OPEN
  reason: Real hardware evidence must be generated by tests/hil/verify_all_public_api_real_hardware.robot
    on the target bench.
  release_effect: D2/P1 not claimed.
