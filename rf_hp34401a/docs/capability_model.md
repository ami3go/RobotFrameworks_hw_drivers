# Capability model

The static model is maintained under `capability/`. Runtime discovery is available through `Get Driver Capabilities`, `Get Driver Capability`, `Find Driver Capabilities`, `Get Driver Features`, `Refresh Driver Capabilities`, and `Validate Driver Capabilities`.
